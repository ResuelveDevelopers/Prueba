<apex:page standardController="Lead"  sidebar="false" showHeader="false">
      <apex:pageBlock >
        <apex:pageBlockTable value="{!Lead.DeudasOportunidades__r}" var="item">
            <apex:column value="{!item.name}"/>
            <apex:column value="{!item.TipoProducto__c}"/>
            <apex:column value="{!item.EntidadFinanciera__c}"/>
            <apex:column value="{!item.Monto__c}"/>
            <apex:column value="{!item.LimiteCredito__c}"/>      
            <apex:column value="{!item.UltimoPago__c}"/>
            <apex:column value="{!item.NumeroCredito__c}"/>  
        </apex:pageBlockTable> 
    </apex:pageBlock> 
</apex:page>